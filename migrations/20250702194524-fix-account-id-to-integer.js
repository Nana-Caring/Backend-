'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    // This migration was already handled in previous migrations
    // No changes needed
  },

  async down(queryInterface, Sequelize) {
    // No rollback needed
  }
};